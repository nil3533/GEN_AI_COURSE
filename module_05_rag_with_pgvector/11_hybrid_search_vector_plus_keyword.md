# Hybrid Search: Vector plus Keyword

Vector search finds semantic similarity. Keyword search finds exact terms.

Hybrid search combines both:

```text
vector results + full-text search results -> merged ranking
```

Use hybrid search when documents contain exact identifiers, product names, error codes, APIs, or legal terms.

For Module 5, start with vector search. Add hybrid search when retrieval misses exact strings.
