# Spring AI Notes

Spring AI ChatClient is the fluent API used by Java and Spring Boot applications to call chat models. It keeps provider details behind a common API so the application code can focus on prompts, options, and responses.

In this course, ChatClient is used after retrieval when the application has already selected the most relevant context chunks.
