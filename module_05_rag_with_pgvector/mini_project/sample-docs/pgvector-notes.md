# pgvector Notes

pgvector is a PostgreSQL extension that stores embeddings in vector columns. A Spring Boot RAG application can store every document chunk with its embedding and then search by vector distance.

This mini-project uses cosine distance through pgvector for similarity search.
