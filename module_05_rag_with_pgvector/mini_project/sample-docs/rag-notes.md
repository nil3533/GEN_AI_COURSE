# RAG Notes

Retrieval-Augmented Generation reduces hallucination by retrieving relevant chunks before the model answers. The application sends the user question plus selected context into the prompt.

RAG still needs citations, evaluation, and careful chunking because retrieval quality controls answer quality.
